# 0003 - 推荐更新机制：事件驱动记录 + 定时重算 + 缓存读取

用户点击/评分写入数据库（事件表），推荐结果由后台定时任务（每 5 分钟）重算并写入推荐缓存表；API 接口只读缓存。避免每次请求全量重算，是工业级标准做法（分级缓存思想）。

**Status**: accepted

**Considered Options**: 纯实时重算（简单但数据量大时扛不住）；纯定时（无事件记录，丢失行为数据）

**Consequences**: 需要引入调度（APScheduler 或 FastAPI 后台任务）；用户感知推荐更新延迟 ≤ 重算周期。
