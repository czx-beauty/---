# 0001 - 混合推荐路线：点击隐式反馈 + 评分显式初始化

用户需求「点击电影学习喜好」，但数据集 ml-latest-small 是显式评分（ratings.csv）。我们决定：主交互为点击（隐式反馈，评分二值化：>=4 视为喜欢），冷启动与算法验证使用历史评分数据。这兼顾了交互的真实感与数据的可用性。

**Status**: accepted

**Considered Options**: 纯评分协同过滤（简单但交互弱）；纯点击建模（数据集缺乏真实点击流，冷启动难）

**Consequences**: 需要同时维护点击事件表与评分表；评分二值化阈值（>=4）可调。
